[project]
name = "my_openenv_project"
version = "0.1.0"
description = "Email triage OpenEnv environment"
authors = [
  { name = "Manav", email = "manav@example.com" }
]

dependencies = [
  "pydantic",
  "fastapi",
  "uvicorn",
  "openai",
  "openenv-core>=0.2.0"
]

# ✅ THIS FIXES SCRIPT ERROR
[project.scripts]
server = "server.app:main"

[tool.setuptools]
packages = ["env", "server"]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"